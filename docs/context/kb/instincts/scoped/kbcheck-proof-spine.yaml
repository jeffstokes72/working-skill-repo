instincts:
  - id: digest-all-proof-check-semantics
    scope: kbcheck/proof-spine
    trigger: "when adding or changing a proof-spine check field that can affect the sensor result"
    behavior: "include that field in the proof check digest and add a regression test proving the digest changes when the field changes"
    confidence: 0.5
    domain: testing
    observations: 1
    first_seen: 2026-07-05
    last_seen: 2026-07-05
    evidence:
      - "2026-07-05 Phoenix proof spine merge review: timeout_ms initially changed command behavior without changing the check digest; fixed in cmd/kbcheck/proof_spine.go and covered in cmd/kbcheck/proof_spine_test.go"
